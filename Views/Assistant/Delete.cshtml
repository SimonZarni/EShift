@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@model EShift123.Models.Assistant
@{
    ViewData["Title"] = "Delete Assistant";
}

<div class="container py-5">
    <h1 class="text-center mb-4 text-danger">Delete Assistant</h1>
    <h3 class="text-center mb-5 text-muted">Are you sure you want to delete this assistant?</h3>

    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0 rounded-3">
                <div class="card-body p-4">
                    <dl class="row mb-4">
                        <dt class="col-sm-4">
                            Name:
                        </dt>
                        <dd class="col-sm-8">
                            @Model.Name
                        </dd>
                        <dt class="col-sm-4">
                            Phone Number:
                        </dt>
                        <dd class="col-sm-8">
                            @Model.Phone
                        </dd>
                    </dl>

                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="AssistantId" />
                        <div class="d-flex justify-content-center mt-4">
                            <button type="submit" class="btn btn-danger me-2 rounded-2 shadow-sm">Delete</button>
                            <a asp-action="Index" class="btn btn-secondary rounded-2 shadow-sm">Back to List</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
